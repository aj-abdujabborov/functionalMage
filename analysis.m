

classdef analysis < matlab.mixin.Copyable % inherits from handle
    properties

    end

    methods
        function result = addDimension(obj, string)
            result = string;
            disp("Dope results");
        end
    end
end